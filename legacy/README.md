# Legacy Code

`app.js` is the original monolithic Dictater application (dictation + spelling only).

It has been **retired** in favor of the modular Vite app in `src/`. This file is kept for reference when porting features.

The active entry point is `src/main.js` via `index.html`.
